services:
  - type: web
    name: sneak
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sneak-db
          property: connectionString
      - key: NODE_ENV
        value: production

databases:
  - name: sneak-db
    plan: free
